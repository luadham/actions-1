name: My First Workflow
on: push
jobs:
    first_job: # Job Name
        runs-on: ubuntu-latest
        steps:
        - name: Welcome message
          run: echo "Hello World"
        - name: List Files
          run: ls
        - name: Read File
          run: cat README.md
